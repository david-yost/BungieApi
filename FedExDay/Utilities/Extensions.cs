using FedExDay.Constants;

namespace FedExDay.Utilities
{
    public static class Extensions
    {
        public static void AddApiKeyHeader(this HttpRequestMessage message)
        {
            //message.Headers.Add(Headers.API_KEY, _apikey);
        } 
    }
}
